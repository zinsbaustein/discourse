_default: &default
  rails:
    configuration:
      operating_system: ubuntu2204
      ruby_version: 3.3.1
      asset_pipeline_precompile: false
      node_version: "20" # will install latest v20.x.x

  postgresql:
    server: external
    configuration:
      tamper_with_yml: false

  redis:
    configuration:
      version: 7.2.4
      operating_system: ubuntu2204

production:
  <<: *default

staging:
  <<: *default
